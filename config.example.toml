# Default (Gemini)
provider = "gemini"
model = "gemini-2.5-flash"
api_key = "YOUR_GEMINI_API_KEY"
temperature = 0.2
timeout_seconds = 15
mode = "stable"

# Optional: Override allowed commands in stable mode
# allowed_commands = ["fastfetch", "ls", "date"]

# Optional: Enable/Disable specific tools
# tools = ["run_shell_command", "search_web"]

# Example for Ollama
# provider = "ollama"
# model = "llama3:8b"
# temperature = 0.2
# timeout_seconds = 15
# mode = "stable"
